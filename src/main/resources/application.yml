server:
  port: 8888

spring:
  profiles:
    active: vault, git
  cloud:
    config:
      server:
        vault:
          order: 1
        git:
          order: 2
          uri: https://github.com/spring-cloud-samples/config-repo
          basedir: target/config
